// enable process
bool particleGun.do = true;

// Limits on cos(polar angle):
double particleGun.czmin =  0.99;
double particleGun.czmax =  0.99;

// Limits on phi angle: default [0, 2pi]
//double particleGun.phimin = 0.0;
//double particleGun.phimax = 0.0;

// Time limits in ns, relative to protons on target.
double particleGun.tmin  =  0.0;
double particleGun.tmax  =  0.0;

// position
vector<double> particleGun.point = {0., 0., 0.};

// shape and size
string particleGun.sourceShape = "cylinder_z";
vector<double> particleGun.halfLength = { 3., 3., 0.0305 }; // 6mm and half of 71 um

// Limits on momentum: (MeV)
double particleGun.pmin = 99.;
double particleGun.pmax = 99.;

//int particleGun.id = 2212;
int particleGun.id = -13; // mu+

// Mean particle.
double particleGun.mean =  -1;

// Control of histograms.
bool particleGun.doHistograms = true;

bool particleGun.verbose = true;

// This tells emacs to view this file in c++ mode.
// Local Variables:
// mode:c++
// End:
